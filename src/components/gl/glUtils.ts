// three.js removed — GL rendering is not supported in Expo Go (SDK 54 New Arch).
// This stub keeps any existing imports from breaking.
export type ExpoWebGLRenderingContext = any;
export function makeRenderer(_gl: ExpoWebGLRenderingContext, _opts?: any): any {
  return null;
}
